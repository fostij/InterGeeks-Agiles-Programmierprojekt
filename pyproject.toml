[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kfz-schadenprognose"
version = "0.9.5"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*", "app*"]